@IsTest
private class {{{clientClassName}}}Test {
  @IsTest
  static void testDefaultConstructor() {
    final {{{clientClassName}}} client = new {{{clientClassName}}}('https://test', 'v1');
    Assert.areNotEqual(null, client);
  }

  @IsTest
  static void testInjectedHttpClient() {
    final {{{httpClientMockClassName}}} mock = new {{{httpClientMockClassName}}}();
    final {{{clientClassName}}} client = new {{{clientClassName}}}('https://test', 'v1', mock);
    Assert.areNotEqual(null, client);
  }

  @IsTest
  static void testAddHeaderReturnsSelf() {
    final {{{clientClassName}}} client = new {{{clientClassName}}}('https://test', 'v1');
    final {{{clientClassName}}} returned = client.addHeader('X-Test', 'value');
    Assert.areEqual(client, returned);
  }

  @IsTest
  static void testSendDelegatesToHttpClient() {
    final {{{httpClientMockClassName}}} mock = new {{{httpClientMockClassName}}}();
    final HttpResponse res = new HttpResponse();
    res.setStatusCode(200);
    res.setBody('{}');
    mock.nextResponse = res;
    final {{{clientClassName}}} client = new {{{clientClassName}}}('https://test', 'v1', mock);
    final {{{httpRequestBuilderClassName}}} builder = new {{{httpRequestBuilderClassName}}}()
      .setEndpoint('x')
      .setMethod({{{httpRequestBuilderClassName}}}.HttpMethod.GET);
    final {{{httpResultClassName}}} result = client.send(builder);
    Assert.areEqual(200, result.response.getStatusCode());
  }
}
