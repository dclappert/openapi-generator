{{accessModifier}} interface {{{authProviderClassName}}} {
    /**
    * Returns authentication headers to apply to the request.
    */
    Map<String, String> getAuthHeaders(final {{{requestContextClassName}}} requestContext);
}